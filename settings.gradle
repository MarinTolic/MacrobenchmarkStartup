pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        /** uncomment the line below in order to use local builds **/
        //mavenLocal()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "MacrobenchmarkStartup"
include ':app'
include ':benchmark'
